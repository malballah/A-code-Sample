using System;
using System.Collections.Generic;
using System.Linq;
using System.Linq.Dynamic;
using System.Web;

namespace Store
{
    public static class Extensions
    {
        //public static IEnumerable<T> SortBy<T>(this IEnumerable<T> items,string field,string type)
        //{
        //    if (string.IsNullOrEmpty(field))
        //        return items.AsEnumerable();
        //    return type == "asc" ? items.OrderBy(field) : items.OrderBy(field + " descending");
        //}
    }
}